<section class="posts">
    {{#each posts as |post|}}
    <div class="post_title">
    <h2>{{post.title}}</h2>
    <h3>{{post.user.username}}</h3>
    </div>
    <div class="post_content">
        <p>{{post.content}}</p>
    </div>
    {{/each}}

</section>